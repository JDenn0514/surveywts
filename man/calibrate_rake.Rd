% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_rake.R
\name{calibrate_rake}
\alias{calibrate_rake}
\title{Rake survey weights to marginal population totals}
\usage{
calibrate_rake(
  data,
  targets,
  weights = NULL,
  wt_name = "wts",
  type = c("prop", "count"),
  algorithm = c("anesrake", "survey"),
  cap = NULL,
  control = list(),
  reference_design = NULL
)
}
\arguments{
\item{data}{A \code{data.frame}, \code{weighted_df}, \code{survey_taylor}, or
\code{survey_nonprob}. \code{survey_replicate} -> error. Any other class -> error.}

\item{targets}{Named list or data frame specifying population margin targets.

\strong{Format A -- named list:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  age_group = c("18-34" = 0.28, "35-54" = 0.37, "55+" = 0.35),
  sex       = c("M" = 0.49, "F" = 0.51)
)
}\if{html}{\out{</div>}}

Each element can be a named numeric vector or a data frame with columns
\code{level} and \code{target} (formats can be mixed within the list).

\strong{Format B -- long data frame} with columns \code{variable}, \code{level}, \code{target}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{data.frame(
  variable = c("age_group", "age_group", "sex", "sex"),
  level    = c("18-34", "35-54", "M", "F"),
  target   = c(0.40, 0.60, 0.49, 0.51)
)
}\if{html}{\out{</div>}}

Format B is auto-detected and converted to Format A before use.}

\item{weights}{<\code{\link[tidyselect:language]{tidy-select}}> Weight column name
(bare name). \code{NULL} -> auto-detected from \code{weighted_df} attribute or
survey object \verb{@variables$weights}. For plain \code{data.frame} with
\code{weights = NULL}, uniform starting weights are used and the output
column is named by \code{wt_name} (default \code{"wts"}).}

\item{wt_name}{Character scalar. Name of the output weight column in the
returned \code{weighted_df}. Default \code{"wts"}. Ignored when \code{data} is a survey
object (\code{survey_taylor} or \code{survey_nonprob}).}

\item{type}{Character scalar. \code{"prop"} (default): \code{targets} values are
proportions. \code{"count"}: \code{targets} values are counts.}

\item{algorithm}{Character scalar. \code{"anesrake"} (default): chi-square
discrepancy variable selection with improvement-based convergence, as in
the \code{anesrake} package. \code{"survey"}: fixed-order IPF cycling through all
targets, with epsilon-based convergence, as in \code{survey::rake()}.}

\item{cap}{Numeric or \code{NULL}. Cap on the weight ratio \code{w / mean(w)}. Any
weight exceeding \code{cap * mean(w)} is set to \code{cap * mean(w)}. Applied
after each per-margin adjustment step (not post-hoc). \code{NULL} (default)
means no cap. Not compatible with \code{algorithm = "survey"} (-> error).}

\item{control}{Named list of algorithm parameters. Merged with
algorithm-specific defaults. Omitted keys retain their defaults.

\strong{\code{algorithm = "anesrake"} defaults:}
\itemize{
\item \code{maxit = 1000}: maximum full sweeps
\item \code{improvement = 0.01}: percentage improvement convergence threshold
\item \code{pval = 0.05}: chi-square p-value threshold for variable selection
\item \code{min_cell_n = 0L}: minimum unweighted observations per cell (0 = no min)
\item \code{variable_select = "total"}: chi-square aggregation for ranking
(\code{"total"}, \code{"max"}, or \code{"average"})
}

\strong{\code{algorithm = "survey"} defaults:}
\itemize{
\item \code{maxit = 100}: maximum full sweeps
\item \code{epsilon = 1e-7}: maximum relative margin error convergence threshold
}

Passing \code{"anesrake"}-specific keys when \code{algorithm = "survey"} (or vice
versa) triggers \code{surveywts_warning_control_param_ignored} per ignored key.}

\item{reference_design}{A \code{survey_taylor} object or \code{NULL}. The probability
survey from which \code{targets} were estimated. When non-\code{NULL}, stored in
the history entry with \code{targets_from_reference = TRUE}. Any non-\code{NULL}
non-\code{survey_taylor} value triggers an error.}
}
\value{
\itemize{
\item \code{data.frame} or \code{weighted_df} input -> \code{weighted_df}
\item \code{survey_taylor} or \code{survey_nonprob} input -> same class as input
(class is preserved)
}

The weight column in the output contains raked weights. A history entry
with \code{operation = "calibrate_rake"} is appended to \code{weighting_history}.
}
\description{
Iterative proportional fitting (raking) that adjusts survey weights to
match multiple marginal population totals simultaneously. Supports two
algorithms: the \code{"anesrake"} method (chi-square variable selection,
improvement-based convergence) and the \code{"survey"} method (fixed-order
IPF, epsilon-based convergence).
}
\details{
\strong{\code{algorithm = "anesrake"}:} At each sweep, variables are sorted by
their chi-square discrepancy (controlled by \code{control$variable_select}).
Variables with any cell below \code{control$min_cell_n} unweighted observations
are excluded entirely. Variables where the chi-square p-value exceeds
\code{control$pval} are skipped in that sweep. Convergence is assessed as the
percentage improvement in total chi-square between consecutive sweeps.
If all variables pass or are excluded in sweep 1, a
\code{surveywts_message_already_calibrated} message is emitted.

\strong{\code{algorithm = "survey"}:} Variables are raked in the fixed order given
by \code{targets}. All variables participate in every sweep. Convergence is
assessed as the maximum relative error across all margin cells falling
below \code{control$epsilon}.
}
\examples{
df <- data.frame(
  age_group = c("18-34", "35-54", "55+", "18-34", "35-54"),
  sex       = c("M", "F", "M", "F", "M"),
  stringsAsFactors = FALSE
)
targets <- list(
  age_group = c("18-34" = 0.40, "35-54" = 0.40, "55+" = 0.20),
  sex       = c("M" = 0.50, "F" = 0.50)
)
result <- calibrate_rake(df, targets = targets)

}
\references{
DeBell, M. and Krosnick, J. A. (2009). \emph{Computing Weights for American
National Election Study Survey Data}. ANES Technical Report series,
no. nes012427. Ann Arbor, MI, and Palo Alto, CA: American National
Election Studies.

Deville, J.-C. and Sarndal, C.-E. (1992). Calibration estimators in
survey sampling. \emph{Journal of the American Statistical Association},
87(418), 376–382. doi:10.2307/2290268

Deville, J.-C., Sarndal, C.-E. and Sautory, O. (1993). Generalized
raking procedures in survey sampling. \emph{Journal of the American
Statistical Association}, 88(423), 1013–1020. doi:10.2307/2290793

Chang, T. and Kott, P. S. (2008). Using calibration weighting to adjust
for nonresponse under a plausible model. \emph{Biometrika}, 95(3), 555–571.
doi:10.1093/biomet/asn022
}
\seealso{
Other calibration: 
\code{\link{calibrate_greg}()},
\code{\link{poststratify}()}
}
\concept{calibration}
