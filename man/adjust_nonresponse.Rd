% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust_nonresponse.R
\name{adjust_nonresponse}
\alias{adjust_nonresponse}
\title{Correct weights for unit nonresponse}
\usage{
adjust_nonresponse(
  data,
  response_status,
  weights = NULL,
  by = NULL,
  wt_name = NULL,
  method = c("weighting-class", "propensity-cell", "propensity"),
  formula = NULL,
  control = list(min_cell = 20, max_adjust = 2, n_cells = 5)
)
}
\arguments{
\item{data}{A \code{survey_taylor} or \code{survey_nonprob}. Must include BOTH
respondents and nonrespondents. \code{survey_replicate} -> error. Any other
class -> error.}

\item{response_status}{Bare name (NSE). Binary response indicator column.
Must be \code{logical} or integer \code{0}/\code{1}. \code{1} / \code{TRUE} = respondent.}

\item{weights}{Bare name (NSE). Weight column. \code{NULL} -> auto-detected from
survey object \verb{@variables$weights}.}

\item{by}{<\code{\link[tidyselect:language]{tidy-select}}> Weighting class variables.
Redistribution is performed within each cell defined by the joint
combination of these variables. \code{NULL} -> global redistribution across
all rows.}

\item{wt_name}{\code{NULL} (default) or a character scalar. When \code{NULL},
adjusted weights overwrite the existing weight column in place. When a
character string, a new column is added and \verb{@variables$weights} updated.}

\item{method}{Character scalar. Adjustment method. One of
\code{"weighting-class"} (default), \code{"propensity-cell"}, or \code{"propensity"}.
\itemize{
\item \code{"weighting-class"}: cells are defined by \code{by} groups; adjustment
factors are applied cell-by-cell.
\item \code{"propensity-cell"}: fits a logistic response propensity model via
\code{formula}, bins scores into \code{control$n_cells} quantile cells, then
applies cell-level adjustments.
\item \code{"propensity"}: fits a logistic response propensity model via \code{formula}
and applies individual-level inverse-probability weights
(\code{weight_i / propensity_i}) to each respondent. Requires \code{formula}.
}}

\item{formula}{A one-sided formula (e.g., \code{~ age_group + sex}) used for
propensity score estimation when \code{method = "propensity-cell"} or
\code{method = "propensity"}. Required for \code{"propensity"}. All variables must
be present in \code{data} with no \code{NA} values.}

\item{control}{Named list of control parameters. Merged with defaults
\code{list(min_cell = 20, max_adjust = 2.0, n_cells = 5)}.
\itemize{
\item \code{min_cell}: warn when a cell has fewer than this many respondents
(default 20, per NAEP methodology). Used only for \code{"propensity-cell"}.
\item \code{max_adjust}: warn when the nonresponse adjustment factor exceeds
this value (default 5.0). For \code{"propensity-cell"}, the cell-level
factor; for \code{"propensity"}, the individual IPW ratio relative to the
mean respondent weight.
\item \code{n_cells}: number of propensity score cells (default 5). Must be a whole
number >= 2. Used only when \code{method = "propensity-cell"}.
Either \code{min_cell} or \code{max_adjust} condition alone triggers the warning.
}}
}
\value{
All rows (respondents and nonrespondents) are returned. Nonrespondent
weights are set to 0; respondent weights are adjusted upward to conserve
the total weight within each cell.
\itemize{
\item \code{survey_nonprob} input -> \code{survey_nonprob} (same class)
\item \code{survey_taylor} input -> \code{survey_taylor} (same class; respondent
rows only, because \code{survey_taylor} does not support zero weights)
}

A history entry with \code{operation = "nonresponse_weighting_class"} (for
\code{method = "weighting-class"}), \code{operation = "nonresponse_propensity_cell"}
(for \code{method = "propensity-cell"}), or \code{operation = "nonresponse_propensity"}
(for \code{method = "propensity"}) is appended to \code{weighting_history}.
}
\description{
Redistributes the weights of nonrespondents to respondents within weighting
classes defined by \code{by}. All rows are returned; nonrespondent weights are
set to zero and respondent weights increase proportionally to preserve the
total weight within each class.
}
\details{
Zero-weight observations are retained for design-based variance estimation.
Survey estimation functions (e.g., \code{\link[survey:svymean]{survey::svymean()}}) handle zero
weights correctly -- zero-weight units are excluded from point estimates
but included in the design structure for variance estimation. For manual
calculations, use \code{w[w > 0]} to exclude nonrespondents.

Diagnostic functions (\code{\link[=effective_sample_size]{effective_sample_size()}}, \code{\link[=weight_variability]{weight_variability()}},
\code{\link[=summarize_weights]{summarize_weights()}}) automatically filter to positive weights before
computing statistics.

Re-calibrating post-nonresponse data requires filtering to respondents
first, because \code{\link[=calibrate]{calibrate()}}, \code{\link[=calibrate_rake]{calibrate_rake()}}, and
\code{\link[=poststratify]{poststratify()}} reject zero weights.

\strong{Propensity-cell method:} A logistic regression is fitted via
\code{\link[stats:glm]{stats::glm()}} with \code{family = binomial}. GLM convergence warnings (e.g.,
fitted probabilities numerically 0 or 1) pass through unchanged. Cell
boundaries are defined by unweighted quantiles of the predicted propensity
scores. The \code{by} argument is silently ignored (a warning is issued).
}
\note{
The propensity-cell method assumes \strong{Missing At Random (MAR)}: response
propensity is fully captured by the formula variables. Violations of MAR
(i.e., response depends on unobserved variables) cannot be detected or
corrected by this method. Additionally, propensity scores are treated as
known (estimated from the data), not as true population propensities; this
understates variance and should be accounted for in downstream analysis.
}
\section{Algorithm}{

Within each cell \eqn{h} defined by \code{by}, the adjustment factor is
\deqn{f_h = \frac{\sum_{i \in h} w_i}{\sum_{i \in h, \text{resp}} w_i}}
where the numerator sums all weights in the cell and the denominator
sums respondent weights only. Each respondent weight becomes
\eqn{w_{i,new} = w_i \times f_h}. Nonrespondent weights are set to 0.
}

\examples{
# survey_taylor: mutate the tibble first, then construct the design -------
gss_with_resp <- gss_2024[!is.na(gss_2024$sex), ]
gss_with_resp$responded <- sample(
  c(0L, 1L), nrow(gss_with_resp), replace = TRUE, prob = c(0.2, 0.8)
)
gss_svy <- surveycore::as_survey(
  gss_with_resp, weights = wtssps, strata = vstrat, ids = vpsu, nest = TRUE
)
result <- adjust_nonresponse(gss_svy, response_status = responded, by = sex)

}
\seealso{
\code{\link[=redistribute_weights]{redistribute_weights()}}

Other nonresponse:
\code{\link[=redistribute_weights]{redistribute_weights()}}
}
\concept{nonresponse}
