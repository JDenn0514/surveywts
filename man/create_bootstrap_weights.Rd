% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replicate-weights.R
\name{create_bootstrap_weights}
\alias{create_bootstrap_weights}
\title{Create bootstrap replicate weights}
\usage{
create_bootstrap_weights(
  data,
  replicates = 500L,
  ...,
  type = c("Rao-Wu-Yue-Beaumont", "Rao-Wu", "Antal-Tille", "Preston", "Canty-Davison"),
  mse = TRUE,
  seed = NULL
)
}
\arguments{
\item{data}{A \code{survey_taylor} or \code{survey_nonprob} design object.}

\item{replicates}{\code{integer(1)}, default \code{500L}. Number of bootstrap
replicates. Must be >= 2. Whole-number doubles are coerced silently.}

\item{...}{Must be empty. Forces all subsequent arguments to be named.}

\item{type}{\code{character(1)}. Bootstrap variant passed to
\code{\link[svrep:as_bootstrap_design]{svrep::as_bootstrap_design()}}. One of \code{"Rao-Wu-Yue-Beaumont"} (default),
\code{"Rao-Wu"}, \code{"Antal-Tille"}, \code{"Preston"}, or \code{"Canty-Davison"}.}

\item{mse}{\code{logical(1)}, default \code{TRUE}. If \code{TRUE}, variance is estimated
as the deviation from the full-sample estimate.}

\item{seed}{\code{integer(1)} or \code{NULL}. If non-\code{NULL}, sets the RNG seed via
\code{\link[withr:with_seed]{withr::local_seed()}} for the duration of the call; caller's RNG state is
restored on exit.}
}
\value{
A \code{survey_replicate} with \code{replicates} new \code{rep_1...rep_N} columns,
\verb{@variables$type = "bootstrap"}, and a \code{"replicate_creation"} entry in the
weighting history.
}
\description{
Generates bootstrap replicate weights via \code{\link[svrep:as_bootstrap_design]{svrep::as_bootstrap_design()}}.
Both \code{survey_taylor} and \code{survey_nonprob} inputs are supported.
}
\seealso{
Other replicate-weights: 
\code{\link{create_jackknife_weights}()}
}
\concept{replicate-weights}
