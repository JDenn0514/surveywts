# Print a weighted data frame

Print a weighted data frame

## Usage

``` r
# S3 method for class 'weighted_df'
print(x, n = 10, ...)
```

## Arguments

- x:

  A `weighted_df` object.

- n:

  Number of rows to show (default 10).

- ...:

  Additional arguments passed to the tibble print method.

## Value

`x`, invisibly.
