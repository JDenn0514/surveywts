% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rescale_weights.R
\name{rescale_weights}
\alias{rescale_weights}
\title{Rescale survey weights to a target mean or sum}
\usage{
rescale_weights(data, weights = NULL, by = NULL, wt_name = NULL)
}
\arguments{
\item{data}{A \code{survey_taylor}, \code{survey_nonprob}, or \code{survey_replicate}. For
inputs carrying replicate weight columns, see the \strong{Replicate Weights}
section.}

\item{weights}{<\code{\link[tidyselect:language]{tidy-select}}> Weight column.
Auto-detected from survey object \verb{@variables$weights}.}

\item{by}{<\code{\link[tidyselect:language]{tidy-select}}> Grouping variable(s).
Rescaling is performed within each group (weights in group \code{h} sum to
\code{n_h}). \code{NULL} → global rescaling (all weights sum to \code{n}).}

\item{wt_name}{\code{NULL} (default) or a \code{character(1)}. When \code{NULL}, rescaled
weights overwrite the existing weight column in place. When a character
string, a new column is added and \verb{@variables$weights} updated.}
}
\value{
An object of the same class as \code{data} with rescaled weights. A
new entry with \code{operation = "rescale_weights"} is appended to the
weighting history.
}
\description{
Rescales weights so they sum to the sample size \code{n} (globally) or to the
group sample size within each group (when \code{by} is specified). Relative
weights within the sample are preserved exactly. Applies to main weights
and — for inputs carrying replicate weight columns (\code{survey_replicate} or
\code{survey_nonprob} with \code{repweights}) — all replicate columns.
}
\details{
The scale factor is \code{n / sum(w)} globally, or \code{n_h / W_h} per group \code{h}.
This operation preserves ratio estimators (means, proportions) but changes
population total estimates by the factor \code{n / W}.
}
\section{Replicate Weights}{

When the input carries replicate weight columns (either \code{survey_replicate}
or \code{survey_nonprob} with \code{repweights}), all replicate columns are scaled by
the same factor(s) derived from the main weights — globally \code{n / sum(w)} or
per group \code{n_h / W_h}. Each row's replicate values are multiplied by the
same scalar (global) or per-group scalar that was applied to the main weight.
}

\examples{
ns_wave1_svy <- surveycore::as_survey_nonprob(ns_wave1, weights = weight)

# Rescale weights to unit mean (default) -----------------------------------
summarize_weights(ns_wave1_svy)

result <- rescale_weights(ns_wave1_svy)
summarize_weights(result)

# Rescale within groups using by = -----------------------------------------
result_by <- rescale_weights(ns_wave1_svy, by = ns_region)
summarize_weights(result_by, by = ns_region)
}
\seealso{
\code{\link[=trim_weights]{trim_weights()}}. For the class system, the standard
workflows, and a glossary of terms, see the \href{https://jdenn0514.github.io/surveywts/articles/getting-started.html}{Getting started article}.

Other utilities:
\code{\link[=trim_weights]{trim_weights()}}
}
\concept{utilities}
