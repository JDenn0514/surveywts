% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_gen_rep_weights.R
\name{create_gen_rep_weights}
\alias{create_gen_rep_weights}
\title{Generate generalized replication replicate weights}
\usage{
create_gen_rep_weights(
  data,
  ...,
  variance_estimator = "SD2",
  max_replicates = Inf,
  balanced = TRUE,
  aux_var_names = NULL,
  mse = TRUE,
  seed = NULL
)
}
\arguments{
\item{data}{A \code{survey_taylor} design.}

\item{...}{Must be empty.}

\item{variance_estimator}{\code{character(1)}. Target variance estimator. Same
options as \code{\link[=create_gen_boot_weights]{create_gen_boot_weights()}}. Default \code{"SD2"}.}

\item{max_replicates}{\code{numeric(1)}, default \code{Inf}. Maximum number of
replicates; \code{Inf} uses the natural count.}

\item{balanced}{\code{logical(1)}, default \code{TRUE}. Equal contribution of
replicates to variance estimates.}

\item{aux_var_names}{\verb{<tidy-select>} or \code{NULL}. Required for
\code{"Deville-Tille"}.}

\item{mse}{\code{logical(1)}, default \code{TRUE}. Centers each replicate deviation
on the full-sample estimate (\code{TRUE}; conservative) or on the mean of
the replicate estimates (\code{FALSE}).}

\item{seed}{\code{integer(1)} or \code{NULL}. RNG seed for reproducibility. The
construction is deterministic unless \code{max_replicates} is below the
fully efficient replicate count; in that case the svrep back-end
retains a random sample of replicates, and the seed makes that draw
reproducible.}
}
\value{
A \code{survey_replicate} with generalized replication weights and
\verb{@variables$type = "other"} (the svrep back-end does not assign a
method-specific type for generalized replication).
}
\description{
Generates Fay's generalized replication weights via
\code{\link[svrep:as_fays_gen_rep_design]{svrep::as_fays_gen_rep_design()}}. Produces deterministic replicate
weights (sets of perturbed weight columns used to compute standard
errors); no randomness is involved. Requires a \code{survey_taylor} design.
}
\details{
\strong{When to use.} Choose generalized replication when you want the
deterministic counterpart of the generalized bootstrap: it is built
from the same target variance estimators, but decomposes the target
matrix into fixed components instead of drawing random multipliers
(Fay 1989). There is no \code{replicates} argument — the construction
sets the count, and \code{max_replicates} caps it. For choosing
\code{variance_estimator}, see the \strong{Choosing a target} section of
\code{\link[=create_gen_boot_weights]{create_gen_boot_weights()}}; note the defaults differ (\code{"SD2"}
here, \code{"SD1"} there).

\strong{Row order.} The default \code{"SD2"} reads the row order of the data.
It assumes the rows are still in the order the sample was drawn in
(Ash 2014). Re-sorted rows give a different and incorrect answer, and
no error is raised. Sort the rows into selection order before you
call this function. \code{"SD1"} reads the row order in the same way; no
other \code{variance_estimator} does.
}
\section{Algorithm}{

Generalized replication (GR) is a BRR extension that removes the
requirement for exactly 2 PSUs per stratum. It constructs
\eqn{R \geq H} replicates (where \eqn{H} is the number of strata)
using a generalized Hadamard matrix, assigning each stratum-PSU unit a
weight that satisfies the BRR variance formula
\deqn{\hat{V}_{GR} = \frac{1}{R} \sum_{r=1}^{R}
  (\hat{\theta}^{(r)} - \hat{\theta})^2.}
Delegates to \code{\link[svrep:as_fays_gen_rep_design]{svrep::as_fays_gen_rep_design()}}.
}

\section{Messages}{

The replicate weight creators re-emit the svrep back end's notices under
their own classes, so you can quiet one without quieting the rest. Which
message you see depends on the function and the arguments.
\itemize{
\item \code{surveywts_message_row_order_assumed} — from
\code{\link[=create_gen_boot_weights]{create_gen_boot_weights()}} and \code{\link[=create_gen_rep_weights]{create_gen_rep_weights()}}, when
\code{variance_estimator} is \code{"SD1"} or \code{"SD2"}. Both estimators read the
row order of the data, and nothing here checks that order.
\item \code{surveywts_message_replicates_rounded_up} — from
\code{\link[=create_sdr_weights]{create_sdr_weights()}}, when the Hadamard matrix order is above
\code{replicates}. The result then carries more replicate columns than you
asked for, and \code{use_normal_hadamard} selects which orders are reachable:
at the default, \code{replicates = 100} gives 128.
\item \code{surveywts_message_replicates_subsampled} — from
\code{\link[=create_gen_rep_weights]{create_gen_rep_weights()}}, when \code{max_replicates} is below the fully
efficient replicate count. The back end keeps a random sample of the
replicates, so set \code{seed} to make the draw reproducible.
\item \code{surveywts_message_backend_note} — from any of them, when the back end
emits a message this package does not recognise. The text is unchanged.
}

To quiet one message and leave the rest alone:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{withCallingHandlers(
  create_sdr_weights(design, replicates = 100L),
  surveywts_message_replicates_rounded_up = function(cnd) \{
    invokeRestart("muffleMessage")
  \}
)
}\if{html}{\out{</div>}}
}

\examples{
# generalized replication with reproducible seed -----------------------
gss_svy <- surveycore::as_survey(
  gss_2024, weights = wtssps, strata = vstrat, ids = vpsu, nest = TRUE
)
# The construction is deterministic at the default `max_replicates = Inf`;
# `seed` only matters below that count, so it is defensive here.
gen_rep_design <- create_gen_rep_weights(gss_svy, seed = 42L)
summarize_weights(gen_rep_design)
# The confidence interval below is computed from the replicate columns
# rather than by Taylor linearization.
surveycore::get_means(gen_rep_design, age)

}
\references{
Fay, R.E. (1984). Some properties of estimates of variance based on
replication methods. \emph{Proceedings of the Section on Survey Research
Methods, American Statistical Association}, 495--500.

Fay, R.E. (1989). Theory and application of replicate weighting for
variance calculations. \emph{Proceedings of the Section on Survey Research
Methods, American Statistical Association}.

Dippo, C., Fay, R.E. and Morganstein, D. (1984). Computing variances
from complex samples with replicate weights. \emph{Proceedings of the
Section on Survey Research Methods, American Statistical Association},
489--494.

Ash, S. (2014). Using successive difference replication for
estimating variances. \emph{Survey Methodology, Statistics Canada},
40(1), 47--59.
}
\seealso{
\code{\link[=create_bootstrap_weights]{create_bootstrap_weights()}}, \code{\link[=create_jackknife_weights]{create_jackknife_weights()}},
\code{\link[=create_brr_weights]{create_brr_weights()}}, \code{\link[=create_gen_boot_weights]{create_gen_boot_weights()}},
\code{\link[=create_sdr_weights]{create_sdr_weights()}}, \code{\link[=create_replicate_weights]{create_replicate_weights()}}, \code{\link[=as_taylor_design]{as_taylor_design()}}.
For the class system, the standard workflows, and a glossary of terms,
see the \href{https://jdenn0514.github.io/surveywts/articles/getting-started.html}{Getting started article}.

Other replicate-weights:
\code{\link[=as_taylor_design]{as_taylor_design()}},
\code{\link[=create_bootstrap_weights]{create_bootstrap_weights()}},
\code{\link[=create_brr_weights]{create_brr_weights()}},
\code{\link[=create_gen_boot_weights]{create_gen_boot_weights()}},
\code{\link[=create_jackknife_weights]{create_jackknife_weights()}},
\code{\link[=create_replicate_weights]{create_replicate_weights()}},
\code{\link[=create_sdr_weights]{create_sdr_weights()}}
}
\concept{replicate-weights}
