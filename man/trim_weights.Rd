% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trim_weights.R
\name{trim_weights}
\alias{trim_weights}
\title{Clip weights to a bounded interval}
\usage{
trim_weights(
  data,
  weights = NULL,
  lower = NULL,
  upper = NULL,
  k = 5,
  type = c("absolute", "percentile"),
  strict = FALSE,
  wt_name = "wts"
)
}
\arguments{
\item{data}{A \code{data.frame}, \code{weighted_df}, \code{survey_taylor}, \code{survey_nonprob},
or \code{survey_replicate}. All survey classes supported. For inputs carrying
replicate weight columns, see the \strong{Replicate Weights} section.}

\item{weights}{<\code{\link[tidyselect:language]{tidy-select}}> Weight column.
Auto-detected for \code{weighted_df} and survey objects. For plain \code{data.frame}
with \code{weights = NULL}, uniform weights (all 1) are used and the output
column is named \code{wt_name}.}

\item{lower}{\code{numeric(1)} or \code{NULL}. Lower bound. \code{NULL} means no lower
trimming. When \code{type = "percentile"}, interpreted as a quantile in
[0, 1]. Must not be \code{NA}.}

\item{upper}{\code{numeric(1)} or \code{NULL}. Upper bound. \code{NULL} with
\code{type = "absolute"} (the default) computes the cutoff as
\code{median(w) + k * IQR(w)}. \code{NULL} is incompatible with
\code{type = "percentile"}. When \code{type = "absolute"}, must be strictly
positive. When \code{type = "percentile"}, must be in [0, 1]. Must not be \code{NA}.}

\item{k}{\code{numeric(1)}. IQR multiplier used only when \code{upper = NULL} and
\code{type = "absolute"}. Default \code{5} (Potter & Zheng 2015). Must be positive.
Ignored when \code{upper} is specified.}

\item{type}{\code{character(1)}. \code{"absolute"} (default): bounds are raw weight
values. \code{"percentile"}: bounds are quantiles on [0, 1].}

\item{strict}{\code{logical(1)}. When \code{FALSE} (default), one clip-and-redistribute
pass is applied. When \code{TRUE}, the loop repeats until all main weights fall
within \verb{[lower_abs, upper_abs]}. Not applied to replicate columns.}

\item{wt_name}{\code{character(1)}. Output weight column name. Used only when
\code{data} is a plain \code{data.frame} with \code{weights = NULL}. Default \code{"wts"}.}
}
\value{
An object of the same class as \code{data} with trimmed weights. A new
entry with \code{operation = "trim_weights"} is appended to the weighting
history.
}
\description{
Clips (trims) survey weights to \verb{[lower, upper]} and redistributes the
trimmed excess equally across untrimmed units, preserving the total weight
sum. Bounds can be absolute weight values or percentiles. Applies to main
weights and — for inputs carrying replicate weight columns
(\code{survey_replicate} or \code{survey_nonprob} with \code{repweights}) — all replicate
columns.
}
\details{
The clip-and-redistribute logic is adapted from \code{survey::trimWeights()}
(Thomas Lumley). The default upper cutoff follows Potter & Zheng (2015):
\code{median(w) + k * IQR(w)} with \code{k = 5}.
}
\section{Replicate Weights}{

When the input carries replicate weight columns (either \code{survey_replicate}
or \code{survey_nonprob} with \code{repweights}), the same absolute bounds
\verb{[lower_abs, upper_abs]} derived from the main weights are applied to each
replicate column using the same clip-and-redistribute logic. The \code{strict}
loop is not applied to replicate columns; each replicate column receives
exactly one clip-and-redistribute pass.
}

\section{Algorithm}{

Bounds are computed from the main weights:
\itemize{
\item \code{type = "absolute"} with \code{upper = NULL}: \code{upper_abs = median(w) + k * IQR(w)};
\code{lower_abs = lower} (or \code{0} if \code{NULL}).
\item \code{type = "absolute"} with \code{upper} specified: \code{upper_abs = upper},
\code{lower_abs = lower} (or \code{0} if \code{NULL}).
\item \code{type = "percentile"}: \code{lower_abs = quantile(w, lower)},
\code{upper_abs = quantile(w, upper)}.
}

Within each clip-and-redistribute pass, weights above \code{upper_abs} are
clipped to \code{upper_abs}; weights below \code{lower_abs} are clipped to
\code{lower_abs}. The clipped mass is redistributed proportionally to
untrimmed observations so that the total weight sum is preserved. When
\code{strict = TRUE}, the pass repeats until all weights satisfy
\verb{[lower_abs, upper_abs]}.
}

\examples{
# IQR default (k = 5) ---------------------------------------------------
trim_weights(ns_wave1, weights = weight)

# explicit percentile bounds --------------------------------------------
trim_weights(
  ns_wave1,
  weights = weight,
  lower = 0.05,
  upper = 0.95,
  type = "percentile"
)

# absolute bounds -------------------------------------------------------
trim_weights(
  ns_wave1,
  weights = weight,
  lower = 0.3,
  upper = 3.0,
  type = "absolute"
)

# survey_replicate — bounds auto-applied to all replicate columns -------
data(cps_2023)
cps_svy <- surveycore::as_survey_replicate(
  cps_2023,
  weights    = "wtfinl",
  repweights = paste0("repwtp", 1:160),
  type       = "successive-difference",
  scale      = 4 / 160,
  rscales    = rep(1, 160)
)
trim_weights(cps_svy)
}
\references{
Potter, F. and Zheng, Y. (2015). Methods and issues in trimming extreme
weights in sample surveys. \emph{Proceedings of the Joint Statistical
Meetings, Section on Survey Research Methods}, 2707--2719.
}
\seealso{
\code{\link[=rescale_weights]{rescale_weights()}}

Other utilities: 
\code{\link{rescale_weights}()}
}
\concept{utilities}
