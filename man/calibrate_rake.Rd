% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_rake.R
\name{calibrate_rake}
\alias{calibrate_rake}
\title{Fit weights using raking}
\usage{
calibrate_rake(
  data,
  targets,
  weights = NULL,
  wt_name = NULL,
  type = c("prop", "count"),
  algorithm = c("classic_ipf", "nr"),
  cap = NULL,
  control = list(),
  reference_design = NULL
)
}
\arguments{
\item{data}{A \code{survey_taylor}, \code{survey_nonprob}, or \code{survey_replicate}. Any
other class -> error. When \code{data} is a \code{survey_replicate}, raking is
applied independently to every replicate weight column using the same
population \code{targets}. Replicate columns that fail raking are kept at their
original values and reported via a
\code{surveywts_warning_replicate_calibration_failed} warning; the full-sample
raking still completes normally.}

\item{targets}{Named list or data frame specifying population margin targets.

\strong{Format A -- named list:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  age_group = c("18-34" = 0.28, "35-54" = 0.37, "55+" = 0.35),
  sex       = c("M" = 0.49, "F" = 0.51)
)
}\if{html}{\out{</div>}}

Each element can be a named numeric vector or a data frame with columns
\code{level} and \code{target} (formats can be mixed within the list).

\strong{Format B -- long data frame} with columns \code{variable}, \code{level}, \code{target}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{data.frame(
  variable = c("age_group", "age_group", "sex", "sex"),
  level    = c("18-34", "35-54", "M", "F"),
  target   = c(0.40, 0.60, 0.49, 0.51)
)
}\if{html}{\out{</div>}}

Format B is auto-detected and converted to Format A before use.}

\item{weights}{<\code{\link[tidyselect:language]{tidy-select}}> Weight column name
(bare name). Auto-detected from survey object \verb{@variables$weights}.}

\item{wt_name}{\code{NULL} (default) or a \code{character(1)}. When \code{NULL}, raked
weights overwrite the existing weight column in place. When a character
string, a new column is added and \verb{@variables$weights} updated.}

\item{type}{Character scalar. \code{"prop"} (default): \code{targets} values are
proportions. \code{"count"}: \code{targets} values are counts.}

\item{algorithm}{Character scalar. \code{"classic_ipf"} (default): chi-square
discrepancy variable selection with improvement-based convergence, as in
the \code{anesrake} package. \code{"nr"}: Newton-Raphson solver using the
multiplicative F-function \code{F(u) = exp(u)}, corresponding to
Deville, Sarndal & Sautory (1993) generalized raking.}

\item{cap}{Numeric or \code{NULL}. Cap on the weight ratio \code{w / mean(w)}. Any
weight exceeding \code{cap * mean(w)} is set to \code{cap * mean(w)}. Applied
after each per-margin adjustment step (not post-hoc). \code{NULL} (default)
means no cap. Not compatible with \code{algorithm = "nr"} (-> error).}

\item{control}{Named list of algorithm parameters. Merged with
algorithm-specific defaults. Omitted keys retain their defaults.

\strong{\code{algorithm = "classic_ipf"} defaults:}
\itemize{
\item \code{maxit = 1000}: maximum full sweeps
\item \code{improvement = 0.01}: percentage improvement convergence threshold
\item \code{pval = 0.05}: chi-square p-value threshold for variable selection
\item \code{min_cell_n = 0L}: minimum unweighted observations per cell (0 = no min)
\item \code{variable_select = "total"}: chi-square aggregation for ranking
(\code{"total"}, \code{"max"}, or \code{"average"})
}

\strong{\code{algorithm = "nr"} defaults:}
\itemize{
\item \code{maxit = 50L}: maximum Newton-Raphson iterations
\item \code{epsilon = 1e-7}: maximum relative margin error convergence threshold
}

Passing \code{"classic_ipf"}-specific keys when \code{algorithm = "nr"} (or vice
versa) triggers \code{surveywts_warning_control_param_ignored} per ignored key.}

\item{reference_design}{A \code{survey_taylor} object or \code{NULL}. The probability
survey from which \code{targets} were estimated. When non-\code{NULL}, stored in
the history entry with \code{targets_from_reference = TRUE}. Any non-\code{NULL}
non-\code{survey_taylor} value triggers an error.}
}
\value{
\itemize{
\item \code{survey_taylor} or \code{survey_nonprob} input -> same class as input
(class is preserved); \verb{@calibration} slot is populated
\item \code{survey_replicate} input -> \code{survey_replicate} (class preserved);
\verb{@calibration} slot is populated, including \code{replicate_converged}
}

The weight column in the output contains raked weights. A history entry
with \code{operation = "calibrate_rake"} is appended to \code{weighting_history}.
For \code{survey_replicate} inputs, each replicate weight column is also
raked. Failed replicates retain their original weights and are recorded
in \code{output@calibration$replicate_converged} as \code{FALSE}.

For \code{algorithm = "nr"}, \verb{@calibration$lambda} contains the converged
Lagrange multiplier vector. For \code{algorithm = "classic_ipf"},
\verb{@calibration$lambda} is \code{NULL}.
}
\description{
Iterative proportional fitting (raking) that adjusts survey weights to
match multiple marginal population totals simultaneously. Supports two
algorithms: the \code{"classic_ipf"} method (chi-square variable selection,
improvement-based convergence, ported from the anesrake package) and the
\code{"nr"} method (Newton-Raphson solver from Deville, Sarndal & Sautory 1993,
using the multiplicative \code{F(u) = exp(u)} function).
}
\section{Algorithm}{

Both algorithms implement the raking calibration function
\eqn{F(u) = \exp(u)}, which keeps all calibrated weights strictly positive.

\strong{Classic IPF (\code{algorithm = "classic_ipf"}, the default)}

At each sweep, variables are ranked by chi-square discrepancy between
weighted and target margins (controlled by \code{control$variable_select},
default \code{"chi_square"}). Variables with any cell below
\code{control$min_cell_n} (default \code{5L}) unweighted observations are excluded
entirely; variables whose chi-square p-value exceeds \code{control$pval}
(default \code{0.01}) are skipped for that sweep. Within each selected
variable, weights are scaled by \code{target_k / weighted_k} for each level
\code{k}. If all variables pass or are excluded in sweep 1, a message is
emitted indicating the data is already calibrated.

\strong{Newton-Raphson (\code{algorithm = "nr"})}

Solves the calibration score equations via Newton-Raphson iteration.
Calibrated weights satisfy
\deqn{w_k = d_k \exp(x_k^T \hat{\lambda})}
where \eqn{\hat{\lambda}} is found by iterating on
\deqn{\sum_{k \in s} d_k \exp(x_k^T \lambda) x_k = X_U.}
Step-halving guards against non-finite g-weights at each iteration.
}

\section{Convergence}{

\strong{Classic IPF:} Terminates when the percentage improvement in total
chi-square discrepancy between successive sweeps falls below
\code{control$improvement} (default \code{0.01}\%), or when \code{control$maxit}
(default \code{1000L}) full sweeps are completed.

\strong{Newton-Raphson:} Terminates when
\eqn{\max(|\text{misfit}| / (1 + |\text{population}|)) < \epsilon},
where \eqn{\epsilon} is \code{control$epsilon} (default \code{1e-7}), or when
\code{control$maxit} (default \code{50L}) iterations are completed.

Calibration non-convergence raises an error.
}

\examples{
ns_wave1_svy <- surveycore::as_survey_nonprob(ns_wave1, weights = weight)

targets_a <- list(
  sex   = c("Male" = 0.49, "Female" = 0.51),
  age_f3 = c("18-34" = 0.30, "35-54" = 0.33, "55+" = 0.37)
)

# Format A + classic_ipf (default) ------------------------------------
calibrate_rake(ns_wave1_svy, targets = targets_a)

# Format A + Newton-Raphson algorithm ---------------------------------
calibrate_rake(ns_wave1_svy, targets = targets_a, algorithm = "nr")

# Format B ------------------------------------------------------------
targets_b <- data.frame(
  variable = c("sex", "sex", "age_f3", "age_f3", "age_f3"),
  level    = c("Male", "Female", "18-34", "35-54", "55+"),
  target   = c(0.49, 0.51, 0.30, 0.33, 0.37)
)
calibrate_rake(ns_wave1_svy, targets = targets_b)
}
\references{
DeBell, M. and Krosnick, J.A. (2009). Computing Weights for American
National Election Study Survey Data. ANES Technical Report series,
no. nes012427. Ann Arbor, MI, and Palo Alto, CA: American National
Election Studies.

Deville, J.-C. and Sarndal, C.-E. (1992). Calibration estimators in
survey sampling. \emph{Journal of the American Statistical Association},
87(418), 376--382.

Deville, J.-C., Sarndal, C.-E. and Sautory, O. (1993). Generalized
raking procedures in survey sampling. \emph{Journal of the American
Statistical Association}, 88(423), 1013--1020.

Kott, P.S. (2003). An overview of calibration weighting. 2003 Joint
Statistical Meetings — Section on Survey Research Methods.
}
\seealso{
\code{\link[=calibrate]{calibrate()}}, \code{\link[=calibrate_linear]{calibrate_linear()}}, \code{\link[=calibrate_logit]{calibrate_logit()}},
\code{\link[=poststratify]{poststratify()}}

Other calibration:
\code{\link[=calibrate]{calibrate()}},
\code{\link[=calibrate_linear]{calibrate_linear()}},
\code{\link[=calibrate_logit]{calibrate_logit()}},
\code{\link[=poststratify]{poststratify()}}
}
\concept{calibration}
