# Implementation — PR 5 — doc-examples-diagnostics-ipw

**Branch**: `docs/examples-diagnostics-ipw`
**Plan**: `plans/doc-examples-overhaul.md` — Task 6 (PR 5, diagnostics and `ipw()`)

## Write surface
- `R/effective_sample_size.R` — modified (`@examples` only: added `#>` comment)
- `R/weight_variability.R` — modified (`@examples` only: added `#>` comment)
- `R/summarize_weights.R` — modified (`@examples` only: added two `#>` blocks)
- `R/ipw.R` — modified (`@examples` only: full rewrite, 111 lines to 45)
- `man/effective_sample_size.Rd` — generated by `devtools::document()`
- `man/weight_variability.Rd` — generated by `devtools::document()`
- `man/summarize_weights.Rd` — generated by `devtools::document()`
- `man/ipw.Rd` — generated by `devtools::document()`
- `tests/testthat/test-example-values.R` — created

## Summary
- Added `#>` expected-output comments to `effective_sample_size()`,
  `weight_variability()`, and both `summarize_weights()` scenarios,
  pasted from real `pkgload::load_all()` output; all values matched
  D5's table with no discrepancy.
- Rewrote `ipw()`'s `@examples` from 111 lines to exactly 45: five
  scenarios (GSS both interfaces, CPS `survey_replicate`, NPORS three
  `missing_method` values, GEE, known population size), each assigning
  and using its result via `summarize_weights()`/`calibrate()`/`nrow()`.
- Closed the `ipw()` GEE dataset gap per D4: replaced inline synthetic
  `data.frame()` construction with `npors_2025_clean` + `wt_pop` +
  `strata = stratum`; deleted `nps_gee`, `ref_gee_df`, `ref_gee`, and the
  direct `surveycore::survey_taylor()` call. No inline `data.frame`
  construction remains in `R/ipw.R`'s examples.
- Seeded the `mice` sub-case via `mice_args = list(seed = 42L)`; kept the
  `requireNamespace("mice", quietly = TRUE)` guard (load-bearing —
  `mice` is not installed in this worktree).
- Removed all four redundant `data()` calls in `R/ipw.R` (`LazyData: true`
  makes them dead code); added `tests/testthat/test-example-values.R`
  guarding all three diagnostics' `#>` comments (ungrouped and
  `by = sex`), 31 assertions, all passing.

## Task checklist
- [x] `effective_sample_size()` — `#>` comment added, bare call kept
- [x] `weight_variability()` — `#>` comment added, bare call kept
- [x] `summarize_weights()` — `#>` block added under both scenarios
- [x] Pasted real printed output; checked against D5's table (all matched)
- [x] `tests/testthat/test-example-values.R` added, rebuilds
      `ns_wave1_svy`, recomputes all three values (ungrouped + `by = sex`),
      asserts at documented precision, failure messages name the file
      and function
- [x] `ipw()` cut to 45 lines (from 111), five scenarios, each assigning
      and using its result
- [x] GEE scenario uses `npors_2025_clean` + `wt_pop` + `strata = stratum`;
      `nps_gee`, `ref_gee_df`, `ref_gee`, and the direct
      `surveycore::survey_taylor()` call deleted
- [x] "Population-scale reference weights" comment replaced with the real
      constraint (`sum(w * x) = sum(d * x)`) and the failure mode (a
      sample-scale reference diverges)
- [x] Downstream: `summarize_weights(result)`, then `calibrate(result,
      targets)` on the primary (GSS) scenario
- [x] `surveywts_warning_ipw_data_na_omitted` shown in a comment on the
      NPORS `"omit"` sub-case only; CPS and GEE scenarios pre-filter
      `race_f4` NA rows in `ns_wave1` so they stay quiet on that warning
- [x] `data(ns_wave1)`, `data(gss_2024)`, `data(cps_2023)`,
      `data(npors_2025_clean)` all removed from `R/ipw.R`
- [x] `requireNamespace("mice", quietly = TRUE)` guard kept
- [x] `mice` sub-case seeded via `mice_args = list(seed = 42L)`
- [x] `devtools::document()` run; `man/*.Rd` committed with source
- [x] `rcmdcheck::rcmdcheck()` clean: 0 errors, 0 warnings, 1 pre-existing
      note (hidden `.git`); 0 examples over 5s; total 21.83s (baseline
      20.70s)

## Signals raised
None. No HOLD.

**One deliberate deviation from a literal reading of Task 6, reported per
instructions:** the GSS scenario (`gss_2024` as reference) is NOT
pre-filtered for its own NA in `sex`/`age_f3`, so
`surveywts_warning_ipw_reference_na_omitted` fires three times across the
block (once per call that uses `gss_ref`), uncommented. Task 6's
checklist only names `surveywts_warning_ipw_data_na_omitted` for the
NPORS "omit" sub-case; it says nothing about this reference-side warning.
Leaving `gss_2024` unfiltered reproduces D6's own audited count exactly
("`surveywts_warning_ipw_reference_na_omitted` (x3) ... gss_2024 has NA
in sex and age_f3 (112 rows)"), so this reads as the plan's intended,
pre-existing behavior rather than a defect to fix — but it does mean the
GSS scenario is not fully silent. D2's claim that the `ipw()` downstream
chain is "silent" is read here as describing the two downstream calls
(`summarize_weights()` + `calibrate()`), not the `ipw()` call itself,
since running it literally shows the reference-side warning firing on
the very first call. Flagging this reading for review rather than
silently resolving it either way.

## CRAN compliance
- [x] TRUE/FALSE used throughout (no T/F)
- [x] `::` used for external calls in all example code
- [x] No bare `print()`/`cat()` in non-print-method code (no changes to
      function bodies at all — `@examples` only)
- [x] No new randomness introduced outside existing `set.seed()`/`seed=`
      usage (`mice_args = list(seed = 42L)` seeds the one previously
      unseeded random call)
- [x] No `par()`/`options()` modified
- [x] No files written by examples
- [x] Examples use at most default (no explicit core count arguments used)
- [x] `devtools::document()` run
- [x] No `installed.packages()`; `requireNamespace()` used for the `mice`
      guard (unchanged from prior code)
- [x] No new `cli_abort()`/`cli_warn()` calls added — no new error/warning
      classes; `plans/error-messages.md` untouched

## Notes for tester
- The `ipw()` `@examples` block is exactly 45 lines (the plan's hard cap),
  counted the way the plan counts: every `#'` line after `@examples` to
  the next roxygen tag. There is no headroom in that count — any future
  edit to `R/ipw.R`'s examples should re-measure rather than assume slack.
- `air format --check` passes on all four touched `R/` files and the new
  test file; roxygen comments are untouched by `air` per
  `code-style.md` §1.
- Ran `devtools::test()` once (full suite): `FAIL 0 | WARN 783 | SKIP 6 |
  PASS 3706`, no regressions. The warning count is pre-existing package
  behavior (unseeded draws, third-party messages), not something this PR
  changed.
- `rcmdcheck::rcmdcheck()` was run once with `_R_CHECK_FORCE_SUGGESTS_=false`
  under `.claude/skills/.../scratchpad`, never inside the repo.

## Standards read
- `.claude/standards/code-style.md`
- `.claude/standards/function-documentation.md`
- `.claude/standards/r-package-conventions.md`
- `.claude/standards/surveywts-conventions.md`
- `.claude/standards/testing-standards.md`
- `.claude/standards/testing-surveywts.md`
