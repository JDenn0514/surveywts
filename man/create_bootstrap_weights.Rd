% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_bootstrap_weights.R
\name{create_bootstrap_weights}
\alias{create_bootstrap_weights}
\title{Generate bootstrap replicate weights}
\usage{
create_bootstrap_weights(
  data,
  replicates = NULL,
  ...,
  type = c("Rao-Wu-Yue-Beaumont", "Rao-Wu", "Antal-Tille", "Preston", "Canty-Davison",
    "quasi-randomization", "hybrid"),
  reference_sample = NULL,
  mse = c("mse", "chrostowski", "uncentered"),
  seed = NULL
)
}
\arguments{
\item{data}{A \code{survey_taylor} or \code{survey_nonprob} design object.
\code{survey_replicate} and \code{data.frame} → error. A \code{survey_nonprob} is
accepted with a probability-sample \code{type} as well; it is then wrapped
as a simple random sample — see \verb{@details}.}

\item{replicates}{\code{integer(1)} or \code{NULL}. Number of bootstrap replicates.
Default \code{NULL} resolves to \code{200L} for \code{type = "quasi-randomization"} and
\code{type = "hybrid"}, and \code{500L} for all probability-sample types. Must be
at least 2. Whole-number doubles are coerced to integer silently.}

\item{...}{Must be empty. Forces all subsequent arguments to be named.}

\item{type}{\code{character(1)}. Bootstrap variant. For probability-sample
designs: \code{"Rao-Wu-Yue-Beaumont"} (default), \code{"Rao-Wu"}, \code{"Antal-Tille"},
\code{"Preston"}, or \code{"Canty-Davison"} — passed to
\code{\link[svrep:as_bootstrap_design]{svrep::as_bootstrap_design()}}. For non-probability samples:
\code{"quasi-randomization"} (resample-reweight bootstrap) or \code{"hybrid"}
(error stub; requires \code{mass_imputation()}, not yet implemented). See
\code{\link[svrep:as_bootstrap_design]{svrep::as_bootstrap_design()}} for how the five probability-sample
variants differ.}

\item{reference_sample}{\code{survey_taylor} or \code{NULL}. Reference probability
sample for NPS types. When non-\code{NULL}, takes precedence over any
reference design stored in \verb{@metadata@weighting_history}. Ignored (with
a warning) when \code{type} is a probability-sample type. \code{survey_replicate}
→ error.}

\item{mse}{\code{character(1)}. Variance formula for bootstrap variance.
\code{"mse"} (default): mean squared deviation from the full-sample estimate,
\eqn{(1/B) \sum (\hat{\theta}^{(b)} - \hat{\theta})^2}.
\code{"chrostowski"}: \eqn{(1/(B-1)) \sum (\hat{\theta}^{(b)} - \hat{\theta})^2}
(NPS types only; errors for probability-sample types). \code{"uncentered"}:
standard Bessel-corrected variance centered on the bootstrap mean.
For probability-sample types, \code{"mse"} maps to \code{TRUE} and \code{"uncentered"}
maps to \code{FALSE} in the \code{svrep} call.
\strong{Legacy note:} \code{mse = TRUE} or \code{mse = FALSE} (logical) is no longer
accepted and emits \code{surveywts_error_mse_not_character}.}

\item{seed}{\code{integer(1)} or \code{NULL}. RNG seed. For NPS types,
\code{set.seed()} is called once immediately before the bootstrap loop (or
before the \code{svrep} pre-computation for Level B). The caller's global RNG
state is \strong{not} restored. For probability-sample types, the seed is
applied via \code{withr::local_seed()} and the caller's state is restored.}
}
\value{
\itemize{
\item Probability-sample types → \code{survey_replicate} with \code{replicates} new
\code{rep_1...rep_N} columns and a \code{"replicate_creation"} history entry.
This holds for \code{survey_nonprob} input too: a \code{survey_nonprob} with a
probability-sample \code{type} returns a \code{survey_replicate}.
\item \code{type = "quasi-randomization"} → \code{survey_nonprob} with \code{replicates}
new \code{repwt_1...repwt_B} columns in \verb{@data}, \verb{@variables$repweights}
populated, and a \code{"bootstrap_weights"} history entry.
}
}
\description{
Generates bootstrap replicate weights (sets of perturbed weight columns
used to compute standard errors) for probability-sample designs via
\code{\link[svrep:as_bootstrap_design]{svrep::as_bootstrap_design()}}. For non-probability samples
(\code{survey_nonprob}), it generates quasi-randomization bootstrap (each
replicate refits the weighting model) replicate weights via an internal
resample-reweight algorithm.
}
\details{
\strong{When to use.} Choose the bootstrap when you estimate a median or
another quantile — the jackknife standard error for a quantile does
not improve as the sample grows (Elliott & Valliant 2017) — or when
your estimator has no textbook variance formula (Wu 2022). For a
non-probability sample with a reference probability sample, use
\code{type = "quasi-randomization"}, which refits the weighting model
inside every replicate.

A \code{survey_nonprob} passed with a probability-sample \code{type} (the default
included) is silently wrapped as a simple random sample: the replicates
resample rows with the base weights and ignore the propensity-estimation
step. To capture that step in the variance, use
\code{type = "quasi-randomization"} instead. The \code{survey_replicate} returned
by the wrapped path cannot be converted back with \code{\link[=as_taylor_design]{as_taylor_design()}} —
that function refuses an object whose source design was a
non-probability sample.
}
\section{Algorithm}{

For probability-sample designs (\code{survey_taylor}), delegates to
\code{\link[svrep:as_bootstrap_design]{svrep::as_bootstrap_design()}} with the specified \code{type}. The variance
estimator for resampling type \code{"Rao-Wu-Yue-Beaumont"} is:
\deqn{\hat{V}_{boot} = \frac{1}{B} \sum_{b=1}^{B}
  (\hat{\theta}^{(b)} - \hat{\theta})^2}
when \code{mse = "mse"}, with \code{B = replicates}.

For non-probability samples (\code{type = "quasi-randomization"}), each
bootstrap replicate resamples respondents with replacement (SRSWR),
then re-runs the original IPW fitting on the resampled data, producing
replicate weights that reflect the variability of the propensity
estimation step.
}

\section{Limitations}{

Bootstrap standard errors from \code{type = "quasi-randomization"} likely
understate true sampling variability because SRSWR resampling cannot
replicate the original NPS recruitment mechanism (AAPOR 2022, §4). This
understatement is not reduced by increasing \code{replicates}.
}

\examples{
# default SRSWR bootstrap on a probability survey -----------------------
gss_svy <- surveycore::as_survey(
  gss_2024, weights = wtssps, strata = vstrat, ids = vpsu, nest = TRUE
)
# Real analyses use more replicates; 100 keeps `R CMD check` fast.
boot_rep <- create_bootstrap_weights(gss_svy, replicates = 100L, seed = 1L)
summarize_weights(boot_rep)

# quasi-randomization bootstrap for a calibrated non-probability sample ----
targets_a <- list(
  sex    = c("Male" = 0.49, "Female" = 0.51),
  age_f3 = c("18-34" = 0.30, "35-54" = 0.33, "55+" = 0.37)
)
ns_wave1_svy <- surveycore::as_survey_nonprob(ns_wave1, weights = weight)
ns_wave1_cal <- calibrate_rake(ns_wave1_svy, targets = targets_a)
# This path refits the weighting model inside every replicate, so it costs
# far more per replicate than the probability bootstrap above. Real
# analyses use more; 10 keeps `R CMD check` fast.
nps_rep <- create_bootstrap_weights(
  ns_wave1_cal,
  type       = "quasi-randomization",
  replicates = 10L,
  seed       = 1L
)
summarize_weights(nps_rep)

}
\references{
Elliott, M.R. and Valliant, R. (2017). Inference for nonprobability
samples. \emph{Statistical Science} \strong{32}(2), 249--264.

Wu, C. (2022). Statistical inference with non-probability survey samples.
\emph{Survey Methodology} \strong{48}(2), 283--311.

Chrostowski, L., Chlebicki, P. and Beresewicz, M. nonprobsvy — An R
package for modern methods for non-probability surveys.
}
\seealso{
\code{\link[=create_jackknife_weights]{create_jackknife_weights()}}, \code{\link[=create_brr_weights]{create_brr_weights()}}, \code{\link[=create_gen_boot_weights]{create_gen_boot_weights()}},
\code{\link[=create_gen_rep_weights]{create_gen_rep_weights()}}, \code{\link[=create_sdr_weights]{create_sdr_weights()}},
\code{\link[=create_replicate_weights]{create_replicate_weights()}}, \code{\link[=as_taylor_design]{as_taylor_design()}}. For the class
system, the standard workflows, and a glossary of terms, see the
\href{https://jdenn0514.github.io/surveywts/articles/getting-started.html}{Getting started article}.

Other replicate-weights:
\code{\link[=as_taylor_design]{as_taylor_design()}},
\code{\link[=create_brr_weights]{create_brr_weights()}},
\code{\link[=create_gen_boot_weights]{create_gen_boot_weights()}},
\code{\link[=create_gen_rep_weights]{create_gen_rep_weights()}},
\code{\link[=create_jackknife_weights]{create_jackknife_weights()}},
\code{\link[=create_replicate_weights]{create_replicate_weights()}},
\code{\link[=create_sdr_weights]{create_sdr_weights()}}
}
\concept{replicate-weights}
