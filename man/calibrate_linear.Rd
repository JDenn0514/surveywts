% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_linear.R
\name{calibrate_linear}
\alias{calibrate_linear}
\title{Fit weights using linear (GREG) calibration}
\usage{
calibrate_linear(
  data,
  targets,
  weights = NULL,
  wt_name = NULL,
  bounds = NULL,
  bounds_scale = c("multiplicative", "absolute"),
  unit_scale = NULL,
  type = c("prop", "count"),
  control = list(),
  reference_design = NULL
)
}
\arguments{
\item{data}{A \code{survey_taylor}, \code{survey_nonprob}, or \code{survey_replicate}. Any
other class -> error. When \code{data} is a \code{survey_replicate}, calibration is
applied independently to every replicate weight column using the same
population \code{targets}. Replicate columns that fail calibration are kept at
their original values and reported via
\code{surveywts_warning_replicate_calibration_failed}; the full-sample
calibration still completes normally.}

\item{targets}{Named list of population marginal targets (Format A) or a
long data frame with columns \code{variable}, \code{level}, \code{target} (Format B).

\strong{Format A -- named list:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  age_group = c("18-34" = 0.30, "35-54" = 0.40, "55+" = 0.30),
  sex       = c("M" = 0.48, "F" = 0.52)
)
}\if{html}{\out{</div>}}

\strong{Format B -- long data frame:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{data.frame(
  variable = c("age_group", "age_group", "sex", "sex"),
  level    = c("18-34", "35-54", "M", "F"),
  target   = c(0.40, 0.60, 0.49, 0.51)
)
}\if{html}{\out{</div>}}

Names must match column names in \code{data}. For \code{type = "prop"}: each
element must sum to 1.0 within 1e-6. For \code{type = "count"}: all values
must be strictly positive and all marginal sums must agree within 1e-3.}

\item{weights}{<\code{\link[tidyselect:language]{tidy-select}}> Weight column name
(bare name). Auto-detected from survey object \verb{@variables$weights}.}

\item{wt_name}{\code{NULL} (default) or a \code{character(1)}. When \code{NULL}, calibrated
weights overwrite the existing weight column in place. When a character
string, a new column is added and \verb{@variables$weights} updated.}

\item{bounds}{\code{NULL} (default) or a length-2 numeric vector \code{c(L, U)}.
\code{NULL}: plain unbounded linear calibration. G-weights are unconstrained
and negative calibrated weights are possible. \code{c(L, U)}: truncated-linear
calibration with g-weights constrained to the closed interval \verb{[L, U]}.
Interpretation depends on \code{bounds_scale}. Triggers
\code{surveywts_error_bounds_invalid_calibration} on invalid values.}

\item{bounds_scale}{Character scalar. \code{"multiplicative"} (default): \code{bounds}
constrain the g-weight ratio \code{g_k = w_k / d_k}. \code{"absolute"}: \code{bounds}
constrain the final calibrated weight \code{w_k} directly. Matched with
\code{rlang::arg_match()}. Ignored when \code{bounds = NULL}. For
\code{"multiplicative"}: requires \verb{L < 1 < U}. For \code{"absolute"}: requires
\verb{0 < L < U}.}

\item{unit_scale}{\code{NULL} (default) or a positive numeric vector of length
\code{nrow(data)}. Per-unit scaling factors \code{q_k} for the calibration distance
function (Deville & Sarndal 1992 eq. 2.2). \code{NULL} is equivalent to
\code{rep(1, nrow(data))}. Triggers \code{surveywts_error_unit_scale_invalid} if
not \code{NULL} and: not numeric, wrong length, contains \code{NA}, or contains
non-positive values.}

\item{type}{Character scalar. \code{"prop"} (default): \code{targets} values are
proportions summing to 1.0 per variable (within 1e-6 tolerance).
\code{"count"}: \code{targets} values are population counts (all strictly positive).
Matched with \code{rlang::arg_match()}.}

\item{control}{Named list of convergence parameters. Merged with defaults
\code{list(maxit = 50, epsilon = 1e-7)}. Unrecognized keys trigger
\code{surveywts_warning_control_param_ignored} per key. Valid keys: \code{maxit},
\code{epsilon}. For plain linear (\code{bounds = NULL}), \code{maxit} and \code{epsilon} are
stored in the history entry but do not affect computation.}

\item{reference_design}{A \code{survey_taylor} object or \code{NULL}. When non-\code{NULL},
stored in the history entry with \code{targets_from_reference = TRUE}. Any
non-\code{NULL} non-\code{survey_taylor} value triggers
\code{surveywts_error_reference_design_not_taylor}.}
}
\value{
\itemize{
\item \code{survey_taylor} input -> \code{survey_taylor} (class preserved); only
\verb{@variables$weights} (the weight column) and \verb{@calibration} are
modified. \verb{@variables$ids}, \verb{@variables$strata}, \verb{@variables$fpc},
and the Taylor design structure are unchanged.
\item \code{survey_nonprob} input -> \code{survey_nonprob} (class preserved); weight
column updated; history entry appended; \verb{@calibration} slot populated.
\item \code{survey_replicate} input -> \code{survey_replicate} (class preserved);
full-sample weight column updated; each replicate weight column
calibrated independently. For \code{type = "count"} targets, each
replicate's population totals are scaled: \code{rep_targets * (sum(rep_wt) / sum(base_wt))}. Failed replicates are kept at original values and
reported via \code{surveywts_warning_replicate_calibration_failed}.
\verb{@calibration} slot populated including \code{replicate_converged}.
}

History entry \code{operation} field: \code{"calibrate_linear"}.
}
\description{
Adjusts survey weights so that the weighted marginal totals of calibration
variables match known population values using linear estimation — GREG
(generalized regression, the survey name for linear calibration). Uses
\code{F(u) = 1 + u}, which is exact in a single Newton step when \code{bounds = NULL}.
When \code{bounds = c(L, U)} is specified, switches to truncated-linear
calibration where g-weights (the ratio of calibrated to starting weight)
are constrained to \verb{[L, U]} using Newton-Raphson iteration.
}
\details{
\strong{When to use.} Choose linear calibration when speed matters: with
\code{bounds = NULL} the solution is exact in one step, with no iteration
(Deville, Sarndal & Sautory 1993). The weight adjustment is
unbounded in both directions, so a large gap between the sample and
the targets can produce negative weights. If negative weights are
unacceptable, set \code{bounds}, or use \code{\link[=calibrate_rake]{calibrate_rake()}} or
\code{\link[=calibrate_logit]{calibrate_logit()}}.
}
\section{Algorithm}{

Linear calibration uses \eqn{F(u) = 1 + u} (the GREG estimator). The
calibrated weights are
\deqn{w_k = d_k (1 + x_k^T \hat{\lambda})}
where the Lagrange multipliers satisfy
\deqn{\hat{\lambda} = \left(\sum_{k \in s} d_k x_k x_k^T\right)^{-1}
  \left(X_U - \sum_{k \in s} d_k x_k\right).}
The solution is obtained in a single Newton step (no iteration required)
when \code{bounds = NULL}.

When \code{bounds = c(L, U)} is specified, g-weights are constrained to
\verb{[L, U]} (truncated-linear calibration) via Newton-Raphson iteration.
}

\section{Convergence}{

For unbounded calibration (\code{bounds = NULL}), the solution is exact in
one step — no convergence check is performed.

For bounded calibration, Newton-Raphson terminates when the maximum
absolute change in \eqn{\lambda} falls below \code{control$epsilon} (default
\code{1e-7}), or when \code{control$maxit} (default \code{50}) iterations are reached.
Calibration non-convergence raises an error.
}

\examples{
ns_wave1_svy <- surveycore::as_survey_nonprob(ns_wave1, weights = weight)

targets_a <- list(
  sex   = c("Male" = 0.49, "Female" = 0.51),
  age_f3 = c("18-34" = 0.30, "35-54" = 0.33, "55+" = 0.37)
)

# Format A ---------------------------------------------------------------
result <- calibrate_linear(ns_wave1_svy, targets = targets_a)
summarize_weights(result)

# Format B ---------------------------------------------------------------
targets_b <- data.frame(
  variable = c("sex", "sex", "age_f3", "age_f3", "age_f3"),
  level    = c("Male", "Female", "18-34", "35-54", "55+"),
  target   = c(0.49, 0.51, 0.30, 0.33, 0.37)
)
calibrate_linear(ns_wave1_svy, targets = targets_b)
}
\references{
Deville, J.-C. and Sarndal, C.-E. (1992). Calibration estimators in
survey sampling. \emph{Journal of the American Statistical Association},
87(418), 376--382.

Deville, J.-C., Sarndal, C.-E. and Sautory, O. (1993). Generalized
raking procedures in survey sampling. \emph{Journal of the American
Statistical Association}, 88(423), 1013--1020.
}
\seealso{
\code{\link[=calibrate]{calibrate()}}, \code{\link[=calibrate_rake]{calibrate_rake()}}, \code{\link[=calibrate_logit]{calibrate_logit()}},
\code{\link[=poststratify]{poststratify()}}. For the class system, the standard workflows, and a
glossary of terms, see the \href{https://jdenn0514.github.io/surveywts/articles/getting-started.html}{Getting started article}.

Other calibration:
\code{\link[=calibrate]{calibrate()}},
\code{\link[=calibrate_logit]{calibrate_logit()}},
\code{\link[=calibrate_rake]{calibrate_rake()}},
\code{\link[=poststratify]{poststratify()}}
}
\concept{calibration}
