% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_greg.R
\name{calibrate_greg}
\alias{calibrate_greg}
\title{Calibrate survey weights to known population totals using GREG}
\usage{
calibrate_greg(
  data,
  targets,
  weights = NULL,
  wt_name = "wts",
  model = c("linear", "logit"),
  type = c("prop", "count"),
  control = list(maxit = 50, epsilon = 1e-07),
  reference_design = NULL
)
}
\arguments{
\item{data}{A \code{data.frame}, \code{weighted_df}, \code{survey_taylor},
\code{survey_nonprob}, or \code{survey_replicate}. Any other class -> error.
When \code{data} is a \code{survey_replicate}, calibration is applied independently
to every replicate weight column using the same population \code{targets}.
Replicate columns that fail calibration are kept at their original values
and reported via a \code{surveywts_warning_replicate_calibration_failed}
warning; the full-sample calibration still completes normally.}

\item{targets}{Named list of population marginal targets (Format A) or a
long data frame with columns \code{variable}, \code{level}, \code{target} (Format B).

\strong{Format A -- named list:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  age_group = c("18-34" = 0.30, "35-54" = 0.40, "55+" = 0.30),
  sex       = c("M" = 0.48, "F" = 0.52)
)
}\if{html}{\out{</div>}}

\strong{Format B -- long data frame:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{data.frame(
  variable = c("age_group", "age_group", "sex", "sex"),
  level    = c("18-34", "35-54", "M", "F"),
  target   = c(0.40, 0.60, 0.49, 0.51)
)
}\if{html}{\out{</div>}}

Names must match column names in \code{data}. For \code{type = "prop"}: each
element must sum to 1.0 within 1e-6. For \code{type = "count"}: all values
must be strictly positive and all marginal sums must agree within 1e-3.}

\item{weights}{<\code{\link[tidyselect:language]{tidy-select}}> Weight column name
(bare name). \code{NULL} -> auto-detected from \code{weighted_df} attribute or
survey object \verb{@variables$weights}. For plain \code{data.frame} with
\code{weights = NULL}, uniform starting weights are used and the output
column is named by \code{wt_name} (default \code{"wts"}).}

\item{wt_name}{Character scalar. Name of the output weight column in the
returned \code{weighted_df}. Default \code{"wts"}. Ignored when \code{data} is a survey
object (\code{survey_taylor} or \code{survey_nonprob}).}

\item{model}{Character scalar. \code{"linear"} (default): one-step exact GREG
calibration (may produce negative weights). \code{"logit"}: bounded iterative
calibration (always positive).}

\item{type}{Character scalar. \code{"prop"} (default): \code{targets} values are
proportions. \code{"count"}: \code{targets} values are counts.}

\item{control}{Named list of convergence parameters. Merged with defaults
\code{list(maxit = 50, epsilon = 1e-7)}. Omitted keys retain their defaults.
Unrecognized keys trigger \code{surveywts_warning_control_param_ignored} per
key. Valid keys are \code{maxit} and \code{epsilon}.}

\item{reference_design}{A \code{survey_taylor} object or \code{NULL}. The probability
survey from which \code{targets} were estimated. When non-\code{NULL}, stored in
the history entry with \code{targets_from_reference = TRUE}. Any non-\code{NULL}
non-\code{survey_taylor} value triggers an error.}
}
\value{
\itemize{
\item \code{data.frame} or \code{weighted_df} input -> \code{weighted_df}
\item \code{survey_taylor} or \code{survey_nonprob} input -> same class as input
(class is preserved); \verb{@calibration} slot is populated
\item \code{survey_replicate} input -> \code{survey_replicate} (class preserved);
\verb{@calibration} slot is populated, including \code{replicate_converged}
}

The weight column in the output contains calibrated weights. A history
entry with \code{operation = "calibrate_greg"} is appended to
\code{weighting_history}.
For \code{survey_replicate} inputs, each replicate weight column is also
calibrated. Failed replicates retain their original weights and are
recorded in \code{output@calibration$replicate_converged} as \code{FALSE}.
}
\description{
Adjusts survey weights so that the weighted marginal totals match known
population values using Generalized Regression (GREG) estimation. Supports
linear (one-step exact) and logit (bounded iterative) calibration models for
categorical auxiliary variables.
}
\examples{
df <- data.frame(
  age_group = c("18-34", "35-54", "55+", "18-34", "35-54"),
  sex = c("M", "F", "M", "F", "M"),
  stringsAsFactors = FALSE
)
targets <- list(
  age_group = c("18-34" = 0.40, "35-54" = 0.40, "55+" = 0.20),
  sex = c("M" = 0.50, "F" = 0.50)
)
result <- calibrate_greg(df, targets = targets)

}
\references{
Deville, J.-C. and Sarndal, C.-E. (1992). Calibration estimators in
survey sampling. \emph{Journal of the American Statistical Association},
87(418), 376–382. doi:10.2307/2290268

Rao, J. N. K., Yung, W. and Hidiroglou, M. A. (2002). Estimating
equations for the analysis of survey data using poststratification
information. \emph{Sankhya}, 64(2), 364–378.

Deville, J.-C., Sarndal, C.-E. and Sautory, O. (1993). Generalized
raking procedures in survey sampling. \emph{Journal of the American
Statistical Association}, 88(423), 1013–1020.
doi:10.1080/01621459.1993.10476369

Valliant, R. (1993). Poststratification and conditional variance
estimation. \emph{Journal of the American Statistical Association},
88(421), 89–96. https://doi.org/10.2307/2290701

Rao, J. N. K., Wu, C. F. J. and Yue, K. (1992). Some recent work on
resampling methods for complex surveys. \emph{Survey Methodology},
18(2), 209–217.
}
\seealso{
Other calibration: 
\code{\link{calibrate}()},
\code{\link{calibrate_poststrat}()},
\code{\link{calibrate_rake}()}
}
\concept{calibration}
