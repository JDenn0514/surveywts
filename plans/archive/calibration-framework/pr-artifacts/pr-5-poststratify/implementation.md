# Implementation: PR 5 — poststratify()

**Branch:** `feature/poststratify`
**Date:** 2026-06-09
**Status:** Complete

---

## Write Surface

### Created
- `R/poststratify.R` — new file; renamed `calibrate_poststrat()` as `poststratify()`
  with history `operation = "poststratify"`, all `{.fn calibrate_poststrat}` error
  message references updated to `{.fn poststratify}`, file header updated
- `man/poststratify.Rd` — generated by `devtools::document()`
- `plans/calibration-framework/pr-5-poststratify/implementation.md` — this file

### Deleted
- `R/calibrate_poststrat.R`
- `man/calibrate_poststrat.Rd` (generated removal)
- `tests/testthat/_snaps/04-poststratify.md` (deleted; regenerated with new function name)

### Modified
- `R/utils.R` — `.format_history_step()`: added `"calibrate_linear"` and
  `"calibrate_logit"` cases (show variable names); removed stale
  `"calibrate_greg"` / `"calibration"` arm; removed `"calibrate_poststrat"`
  fallthrough (only `"poststratify"` remains)
- `R/adjust_nonresponse.R` — updated `@details` broken link from
  `[calibrate_poststrat()]` to `[poststratify()]`
- `tests/testthat/test-04-poststratify.R` — renamed all `calibrate_poststrat(`
  calls to `poststratify(`; updated history operation assertions from
  `"calibrate_poststrat"` to `"poststratify"`; removed test 42 (old API
  regression guard no longer applicable); updated file header comment
- `tests/testthat/_snaps/00-classes.md` — snapshot updated (stale
  `"calibration"` arm removed from `.format_history_step()`; display changes
  from `calibration (variables: age_group)` to `calibration`)
- `tests/testthat/_snaps/replicate-print.md` — same snapshot update reason
- `plans/error-messages.md` — updated four "Thrown by" entries:
  `calibrate_greg()` section header → `calibrate_linear() / calibrate_logit()`;
  all `calibrate_greg()` thrown-by cells → `calibrate_linear()`,
  `calibrate_logit()`; `calibrate_poststrat()` section header → `poststratify()`;
  all `calibrate_poststrat()` thrown-by cells → `poststratify()`;
  "See also" row updated from `calibrate_greg()` → `calibrate_linear()`
- `NAMESPACE` — `export(calibrate_poststrat)` removed; `export(poststratify)` added

---

## Summary

- Renamed `calibrate_poststrat()` to `poststratify()` with no behavioral changes
  to the algorithm (cell-ratio post-stratification, survey_replicate path, and
  `@calibration` slot population are identical)
- History `operation` string changed from `"calibrate_poststrat"` to `"poststratify"`
- `.format_history_step()` in `utils.R` updated: `"calibrate_linear"` and
  `"calibrate_logit"` now display `"{op} (variables: {vars})"` like the raking
  case; stale `"calibrate_greg"` / `"calibration"` arm removed
- `plans/error-messages.md` Thrown-by columns corrected throughout to reflect
  the `calibrate_greg` → `calibrate_linear`/`calibrate_logit` rename and the
  `calibrate_poststrat` → `poststratify` rename
- All 2910 tests pass; 0 failures; snapshots for changed print output reviewed
  and accepted

---

## Task Checklist

- [x] `R/poststratify.R` created with `operation = "poststratify"`, all function
      name references updated
- [x] `R/calibrate_poststrat.R` deleted
- [x] `R/utils.R` `.format_history_step()` updated: `"calibrate_linear"` and
      `"calibrate_logit"` cases added; `"calibrate_greg"` / `"calibration"` arm
      removed; `"calibrate_poststrat"` fallthrough removed
- [x] `tests/testthat/test-04-poststratify.R` updated: all `calibrate_poststrat(`
      → `poststratify(`; operation assertions updated; test 42 removed; header
      comment updated
- [x] `tests/testthat/_snaps/04-poststratify.md` deleted (regenerated by test run)
- [x] `plans/error-messages.md` four "Thrown by" entries updated
- [x] `devtools::document()` run; `NAMESPACE` and `man/` in sync
- [x] Full test suite: 0 FAIL, 2910 PASS

---

## CRAN Compliance Checklist

1. [x] `TRUE`/`FALSE` used throughout (no `T`/`F`)
2. [x] `::` used for all external calls (no `@importFrom` except S3 registration)
3. [x] No bare `print()`/`cat()` in non-print-method code
4. [x] No randomness — `seed = NULL` arg not applicable
5. [x] No `par()`/`options()` modification
6. [x] No file writing
7. [x] No parallelism in examples/tests
8. [x] `devtools::document()` run — NAMESPACE and man/ updated
9. [x] No `installed.packages()` usage
10. [x] All `cli_abort()`/`cli_warn()` have `class=`; classes exist in
        `plans/error-messages.md`

---

## Notes for Tester

- The two snapshot failures in `test-00-classes.md` and `test-replicate-print.md`
  were caused by removing the stale `"calibration"` arm from `.format_history_step()`.
  History entries with `operation = "calibration"` (from old `calibrate()` dispatcher
  tests) now display `"calibration"` without variable names. Snapshots were accepted
  via `testthat::snapshot_accept()` after verifying the diffs matched spec intent.
- The `poststratify.R` file contains inline `# nocov` guards on the
  `surveywts_error_empty_stratum` branch (reachable only in replicate path when a
  cell has zero total replicate weight). This branch was `# nocov` in
  `calibrate_poststrat.R` as well, for the same reason.
- `grep -r "calibrate_poststrat" R/ tests/` returns only inline comments
  (historical notes), no functional references.
