# docs(docs): land the Section H quick-win documentation fixes

**Date**: 2026-08-31
**Branch**: docs/section-h-quick-wins
**Phase**: Documentation improvements (plans/doc-improvements.md, Section H)

## Changes

- Fix the `trim_weights()` "equally" vs. "proportionally" contradiction: the
  code adds a constant amount per unit, so the Algorithm section now says
  "equally"
- Define `n`, `n_positive`, `n_zero`, and `ess` in `summarize_weights()`
  `@returns`; note that zero-weight rows are excluded before the summary, so
  `n_zero` is always 0; stop restating a shorter column list in
  `@description`
- Add the Kish (1965) `@references` to `weight_variability()` and a matching
  entry to `.claude/reference-map.yaml`
- Add a `@section Warnings` to `as_taylor_design()`: every successful
  conversion warns that replicate weights are discarded
- Give `mse` a behavioral description in `create_brr_weights()`,
  `create_gen_boot_weights()`, `create_gen_rep_weights()`, and
  `create_sdr_weights()`
- Add the Tier 4 `@details` method overview and a verified `@references`
  block to `create_replicate_weights()`, adapted from
  `plans/comprehension-replicate-methods.md`
- Document the class-dependent row retention of `adjust_nonresponse()` in a
  named "Input class behavior" section; make the two unconditional
  "all rows are returned / zero-weight rows are retained" sentences
  conditional on input class
- Document the silent `survey_nonprob` pass-through in
  `create_bootstrap_weights()` (`@details`, `@param data`, `@returns`):
  probability types wrap the data as an SRS design, and `as_taylor_design()`
  refuses the result
- Reword `redistribute_weights()` `@description`: the `reduce_if` rows are
  removed, not kept at zero weight; move the internal call-site note from the
  roxygen to a source comment
- Move the `calibrate_to_survey()` svrep migration note to `NEWS.md`;
  internalize `control_col_matches` (removed from `@param control`, the
  Algorithm section, and the Warnings section)
- State `@variables$type = "other"` in `create_gen_rep_weights()` `@returns`
  (run-verified) and explain what `seed` does for a deterministic method;
  clarify that gen-boot's `"bootstrap"` type is the stored type, not the
  method name
- Correct the citations shipped in roxygen: replace the fabricated
  Chrostowski entry and drop the wrong Kolenikov entry in
  `create_bootstrap_weights()`; complete the Fay/Dippo venues, remove the
  duplicated Fay (1989) page range, and add the Bellhouse pages in the
  BRR/gen-boot/gen-rep blocks
- Switch the `pew_2016_optin` doc example to
  `surveycore::as_survey_nonprob()` with `repweights` (run-verified)
- Remove stale references: `create_group_jackknife_weights()` comments in
  three files, deleted `*_svy` dataset comments in `R/data.R`, the orphaned
  `data-raw/acs-wy-2022.R`
- Rewrite the `NEWS.md` Datasets section against verified dataset shapes;
  fix the stale `ipw()` snippet
- Fix and redesign the broken `README.Rmd` chunks (the calibrate-to-survey
  demo now benchmarks the IPW-weighted `ns_wave1` panel to the NPORS
  partisanship estimate; run-verified end-to-end) and re-render `README.md`,
  closing the blocked Section G item

## Files Modified

- `R/adjust_nonresponse.R` — class-dependent row retention promoted to a named section; unconditional sentences fixed
- `R/as_taylor_design.R` — Warnings section added
- `R/calibrate_to_survey.R` — migration note removed; `control_col_matches` internalized
- `R/create_bootstrap_weights.R` — pass-through documented; citations corrected
- `R/create_brr_weights.R` — `mse` prose; citation venues corrected
- `R/create_gen_boot_weights.R` — `mse` prose; type clarifier; citations corrected
- `R/create_gen_rep_weights.R` — `mse`/`seed` prose; `@returns` type; citations corrected
- `R/create_sdr_weights.R` — `mse` prose
- `R/create_replicate_weights.R` — `@details` and `@references` added
- `R/redistribute_weights.R` — description reworded; developer note moved to source comment
- `R/summarize_weights.R` — column definitions; description fixed
- `R/trim_weights.R` — Algorithm wording fixed
- `R/weight_variability.R` — `@references` added
- `R/data.R` — pew example fixed; stale header comments removed
- `R/jackknife-dagjk-utils.R`, `R/replicate-utils.R` — stale function-name comments fixed
- `tests/testthat/helper-test-data.R` — stale function-name comment fixed
- `NEWS.md` — Datasets section rewritten; migration note added; `ipw()` snippet fixed
- `README.Rmd` / `README.md` — chunks fixed and redesigned; re-rendered
- `data-raw/acs-wy-2022.R` — deleted (orphaned builder for a removed dataset)
- `.claude/reference-map.yaml` — `weight_variability` entry added
- `man/*.Rd`, `DESCRIPTION` — regenerated by `devtools::document()`
- `plans/doc-improvements.md` — Section H checklist marked done; statuses updated
