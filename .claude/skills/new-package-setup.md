# New R Package Setup — Surveyverse Standard

**This skill has been moved to the user-level skills directory so it runs from
a safe parent directory rather than from inside surveycore.**

**Canonical location:** `/Users/jacobdennen/.claude/skills/new-package-setup.md`

This file is intentionally left as a stub. Invoke the skill from
`/Users/jacobdennen/` (or any directory that is not inside a git repo) so that
the new package is cloned to a clean location.
