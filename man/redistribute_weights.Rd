% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redistribute_weights.R
\name{redistribute_weights}
\alias{redistribute_weights}
\title{Transfer weight from excluded rows to retained rows}
\usage{
redistribute_weights(
  data,
  reduce_if,
  increase_if,
  weights = NULL,
  by = NULL,
  wt_name = NULL,
  control = list()
)
}
\arguments{
\item{data}{A \code{survey_taylor} or \code{survey_nonprob}. \code{survey_replicate} ->
error. Any other class -> error.}

\item{reduce_if}{Bare name (NSE). Binary indicator column (\code{logical} or
integer \code{0}/\code{1}). Rows where this is \code{TRUE}/\code{1} have their weight set
to 0 and their weight redistributed.}

\item{increase_if}{Bare name (NSE). Binary indicator column. Rows where
this is \code{TRUE}/\code{1} receive the redistributed weight.}

\item{weights}{Bare name (NSE). Weight column. Auto-detected from
\verb{@variables$weights}.}

\item{by}{<\code{\link[tidyselect:language]{tidy-select}}> Grouping variable(s).
Redistribution is performed within each group. \code{NULL} -> global
redistribution.}

\item{wt_name}{\code{NULL} (default) or a character scalar. When \code{NULL},
adjusted weights overwrite the existing weight column in place. When a
character string, a new column is added and \verb{@variables$weights} updated.}

\item{control}{Named list of warning thresholds. Merged with defaults
\code{list(min_cell = 20, max_adjust = 2.0)}. \code{min_cell}: warn when a
group has fewer than this many \code{increase_if} rows. \code{max_adjust}: warn
when the adjustment factor exceeds this value.}
}
\value{
\itemize{
\item \code{survey_nonprob} or \code{survey_taylor} input -> same class as input,
with \code{reduce_if} rows removed (zero weights violate survey validators).
}

A history entry with \code{operation = "redistribute_weights"} is appended.
}
\description{
Sets the weights of rows satisfying \code{reduce_if} to zero and proportionally
redistributes their weight to rows satisfying \code{increase_if} within groups
defined by \code{by}. Rows matching neither condition are unchanged.
}
\details{
This function is the general form of
\link[=adjust_nonresponse]{adjust_nonresponse(method = "weighting-class")}.
When \verb{reduce_if = nonrespondent indicator} and \verb{increase_if = respondent indicator}, the two produce equivalent results (within \code{1e-10}).
\code{adjust_nonresponse()} does not call \code{redistribute_weights()} internally
because it is currently the only call site; refactor if a second emerges.
}
\section{Algorithm}{

Within each group \eqn{h} defined by \code{by}, the adjustment factor applied to
each \code{increase_if} row is
\deqn{f_h = \frac{W_{h,\text{reduce}} + W_{h,\text{increase}}}{W_{h,\text{increase}}}}
where \eqn{W_{h,\text{reduce}}} and \eqn{W_{h,\text{increase}}} are the
summed weights of \code{reduce_if} and \code{increase_if} rows in group \eqn{h}.
Each \code{increase_if} weight becomes \eqn{w_{i,new} = w_i \times f_h}.
Rows matching neither indicator are unchanged.
}

\examples{
# survey_taylor: mutate the tibble first, then construct the design -------
gss_excl <- gss_2024[!is.na(gss_2024$sex), ]
gss_excl$excluded <- sample(
  c(0L, 1L), nrow(gss_excl), replace = TRUE, prob = c(0.8, 0.2)
)
gss_excl$retained <- as.integer(!gss_excl$excluded)
gss_svy <- surveycore::as_survey(
  gss_excl, weights = wtssps, strata = vstrat, ids = vpsu, nest = TRUE
)
redistribute_weights(
  gss_svy, reduce_if = excluded, increase_if = retained, by = sex
)

}
\seealso{
\code{\link[=adjust_nonresponse]{adjust_nonresponse()}}

Other nonresponse: 
\code{\link{adjust_nonresponse}()}
}
\concept{nonresponse}
